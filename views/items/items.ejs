<%- include ('../partials/head.ejs') %>
        <div id="header-area">
            <%- include ('../partials/header.ejs') %>
            <!-- Start select#buttons CSS: investment.css -->
            <section id="buttons">
                <a href="/new/item">Novo item</a>
            </section>
            <!-- Fim select#buttons CSS: investment.css -->
            
            <!-- Start article#total-area CSS: style.css -->
            <article style="width: 100%; margin-bottom: 10px;" id="total-area">
                <aside>
                    <section style="margin: 0;">
                        <h3>Todos os itens ao lado</h3>
                    </section>
                </aside>
                <div id="table-area" style="flex-grow: 12;">
                    <table>
                        <tr>
                            <th>ID</th>
                            <th>Item</th>
                            <th>Categoria</th>
                            <th>Ações</th>
                        </tr>
                        <% items.forEach(item => { %>
                            <tr>
                                <td><%= item.id %></td>
                                <td><%= item.name %></td>
                                <td><%= item.category.name %></td>
                                <td>
                                    <a class="edit" href="/edit/item/<%= item.id %>">Editar</a>
                                    <form method="post" action="/delete/item" style="display: inline;" onsubmit="confirmDeletion(event, this)">
                                        <input type="hidden" name="id" value="<%= item.id %>">
                                        <input type="submit" class="delete" value="Deletar">
                                    </form>
                                </td>
                            </tr>
                        <% }); %>
                    </table>
                </div>
            </article>
            <div style="clear: both;"></div>
            <div class="pagination" style="width: 80%; margin: 0 0 100px 235px;">
                <% if(num > 1) { %>
                    <form id="form" action="/items/page/<%= num - 1 %>" method="get">
                        <input id="sub" style="float: left;" type="submit" value="<<< Anterior">
                    </form>
                <% } %>

                <% if(next) { %>
                    <form id="form" action="/items/page/<%= num + 1 %>" method="get">
                        <input id="sub" style="float: right;" type="submit" value="Próximo >>>">
                    </form>
                <% } %>
            </div>
            <!-- Fim article#total-area CSS: style.css -->
        </div>
        <%- include ('../partials/footer.ejs') %>